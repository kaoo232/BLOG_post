
<header>

	<nav>
	<br/><br/>
		<a href="/" class="nav-item" title="The Blueprint">
			<span class="icon-placeholder">⌂</span> 
			<span class="nav-text">Home</span>
		</a><!--
		<a href="/archive" class="nav-item" title="Fictional Blog Posts">
			<span class="icon-placeholder">⧉</span> 
			<span class="nav-text">The Records</span>
		</a>
		<a href="/gallery" class="nav-item" title="Photography — Let the places speak">
			<span class="icon-placeholder">⧇</span> 
			<span class="nav-text">Observations</span>
		</a>
		<a href="/art" class="nav-item" title="Minimalist Artwork — The drawn private spaces">
			<span class="icon-placeholder">✎</span> 
			<span class="nav-text">The Drawings</span>
		</a>
		<a href="/about" class="nav-item" title="Manifesto">
			<span class="icon-placeholder">⎔</span> 
			<span class="nav-text">Manifesto</span>
		</a>
-->
	</nav>

	<button id="theme-toggle" aria-label="Toggle Mode">
		<span class="icon-placeholder">☾</span>
	</button>
</header>

<script>
	// Handles the theme toggle logic smoothly across the site
	const button = document.getElementById('theme-toggle');
	
	if (button) {
		button.addEventListener('click', () => {
			document.body.classList.toggle('dark');
		});
	}
</script>

<style>
	header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		padding: 1.2rem 0;
		margin-bottom: 1rem;
		border-bottom: var(--border); /* Optional: A solid vintage comic line divider */
	}

	nav {
		display: flex;
		gap: 1.2rem;
		flex-wrap: wrap; /* Ensures responsiveness if the magazine titles expand */
	}

	.nav-item {
		font-family: 'Special Elite', serif; 
		font-size: 0.95rem; /* Slightly balanced for more items */
		font-weight: bold;
		color: var(--ink); 
		text-decoration: none;
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.3rem 0.6rem;
		border: 1px solid transparent; /* Prevents layout shifting on hover */
		transition: color 0.2s ease, background-color 0.2s ease, border-color 0.2s ease;
	}

	.nav-item:hover {
		color: var(--red); 
		background-color: var(--paper-dark); 
		border-color: var(--ink); /* Adds a sharp, thin print border on hover */
	}

	.icon-placeholder {
		font-size: 1.1rem;
	}

	/* Responsive adjustment for mobile viewports */
	@media (max-width: 600px) {
		header {
			flex-direction: column;
			gap: 1rem;
			align-items: flex-start;
		}
		
		nav {
			gap: 0.6rem;
		}
		
		#theme-toggle {
			align-self: flex-end;
		}
	}

	/* The Retro/Comic Button Mutation */
	button {
		background: var(--yellow); 
		border: var(--border); 
		box-shadow: 3px 3px 0px var(--ink); 
		border-radius: 4px; 
		width: 38px;
		height: 38px;
		color: var(--ink);
		font-size: 1.1rem;
		cursor: pointer;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: transform 0.1s cubic-bezier(.22,.68,0,1.3), box-shadow 0.1s ease, background-color 0.2s ease;
	}

	button:hover {
		background: var(--red); 
		color: var(--cream);
		transform: translate(-2px, -2px); 
		box-shadow: 5px 5px 0px var(--ink); 
	}
	
	button:active {
		transform: translate(1px, 1px); 
		box-shadow: 1px 1px 0px var(--ink);
	}
</style>